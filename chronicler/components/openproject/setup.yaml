schema_version: 1
profiles: [full]
depends_on: [postgres, caddy]
conflicts_with: []
platforms: [windows, linux, darwin]
resources: {memory_mb: 1024, disk_mb: 2048}
fields:
  - {key: OPENPROJECT_SECRET_KEY_BASE, label: OpenProject 加密密钥, kind: secret, required: true, generate: token}
readiness: {kind: container-health, timeout_sec: 600}