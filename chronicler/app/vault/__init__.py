"""秘密库（vault）：admin 统一管理全部秘密与秘密文件。

设计依据：ADR-0041（主密钥 + age 加密快照）、ADR-0042（文件型秘密）、
ADR-0043（分权与审计）、ADR-0044（多接收者密钥体系，二期）。

原则：值永远以 age 密文存储；元数据（名称/作用域/属主/过期时间/摘要）透明可见；
一切存取写审计且不含秘密值；admin 可随时全量导出（明文清单 + 密文负载）。
"""
