# 任务：代码结构洞察（code-insight）

你是一名资深架构师。请分析位于 `{{repo_dir}}` 的工程「{{project_name}}」（当前目录即为仓库根）。

## 可用资源（已配置组件，按需使用）
{{components}}

## 要求

1. 通读仓库结构与关键源码（优先读 README/AGENTS.md/入口模块），不要臆测。
2. 产出一份结构洞察报告，包含：
   - 一句话定位与模块地图（目录 → 职责）
   - 关键数据流/控制流
   - 明显的技术债、重复实现或坏味道（带文件路径证据）
   - 3 条以内最高优先级的改进建议
3. **将完整报告以 Markdown 写入文件 `{{report_file}}`**（这是唯一的交付物，不要只打印到 stdout）。

日期：{{date}}

{{extra}}
