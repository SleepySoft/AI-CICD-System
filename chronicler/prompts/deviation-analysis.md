# 任务：需求偏差分析（deviation-analysis）

你是一名严谨的 QA 架构师。分析位于 `{{repo_dir}}` 的工程「{{project_name}}」（当前目录即为仓库根）。

## 可用资源（按能力类别使用，勿假设具体组件必然存在）
{{components}}

若上方某资源给了 SKILL 文件路径，决定使用它时先读该文件再操作；未列出的能力类别视为不可用。

## 要求

1. 找到需求事实源：优先读 `docs/requirements/`（带 ID 的需求条目）；不存在则用 README/设计文档代替并注明。
2. 逐条核对：每条需求标注 已实现 / 部分实现 / 未实现，给出代码证据（文件:行 或提交）。
3. 产出 gap 清单：按严重度排序（阻塞/重要/次要），每条含需求 ID、现状、建议。
4. **将完整分析报告以 Markdown 写入文件 `{{report_file}}`**（唯一交付物）。

日期：{{date}}

{{extra}}
