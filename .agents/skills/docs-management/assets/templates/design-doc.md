# <模块/主题名> — <why|what|how 层一句话说明>

> 版本：v0.1 · 日期：YYYY-MM-DD · 状态：草稿|生效|过时
> 定位：<这篇文档回答什么问题；明确不覆盖什么（Non-Goals 边界）>
> 关联需求：<BR/UR/FR/NFR ID 列表，只引用 ID>

---

## 1. WHY — 动机与原则

<3 行内说清：为什么需要这个模块/机制，解决什么问题；列出适用需求 ID>
<why/ 层文件：本节即正文主体，展开愿景、原则、被否决的方向；what/how 各给一段摘要并链接对应文件>

## 2. WHAT — 规格与契约

<外部可见的规格：数据模型、API 契约、配置项、行为承诺>
<what/ 层文件：本节即正文主体；why 给 3 行摘要，how 只留链接>

## 3. HOW — 实现机制

<内部实现：架构、流程、关键算法、选型机制>
<how/ 层文件：本节即正文主体；why/what 各给 3 行摘要>

## 4. 决策与备选（如适用）

| 决策点 | 选择 | 被否决方案及原因 | ADR |
|--------|------|----------------|-----|
| | | | docs/adr/NNNN-*.md |

---

<how/ 层架构图建议按 C4 缩放：Context → Container → Component；图用 ASCII 或 Mermaid>
